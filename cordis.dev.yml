# Dev overlay: load every plugin in this repo into a dsh boot by absolute path.
#   cd ~/github/deepseek-harness && pnpm dsh web --patch /Users/tali/github/tali-dash-plugins/cordis.dev.yml
# Paths MUST be absolute (a patch contributes config; it does not move the
# loader's module-resolution root). Machine-specific by design.
- insert:
    - id: tali-agent-status-indicator
      name: '/Users/tali/github/tali-dash-plugins/plugins/agent-status-indicator/index.js'
    - id: tali-browser-automation
      name: '/Users/tali/github/tali-dash-plugins/plugins/browser-automation/index.js'
